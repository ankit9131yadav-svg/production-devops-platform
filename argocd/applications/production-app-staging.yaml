apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: production-app-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ankit9131yadav-svg/Production_app.git
    targetRevision: main
    path: helm/production-app
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: production-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
